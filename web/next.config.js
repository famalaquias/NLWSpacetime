/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'localhost',
      'http://localhost:3334',
    ],
  },
}

module.exports = nextConfig
